import { CategoryClassDirective } from './category-class.directive';

describe('CategoryClassDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryClassDirective();
    expect(directive).toBeTruthy();
  });
});
